# university

# Start development:
`docker compose up -d`

### Stop development:
`docker compose down`

### For vscode users:
Run start development command then restart vscode to enable eslint/prittier features